resources:
  Table:
    compare:
      mode: soft
      inherit: true
      fields:
        Name:
          mode: strict
        KeySchema:
          mode: strict
        SEESpecification:
          mode: strict
          inherit: false
types:
  KeySchema:
    compare: